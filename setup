1. 在GitHub上面 创建一个空的代码仓库 创建成功之后复制 代码仓库的地址 
 如：  https://github.com/XuYing123-X/TestGit.git
2. 然后在本地 新建一个空文件夹  右击鼠标  选择 svn检出
   从GitHub上拉取创建的代码仓库
3. 然后在代码仓库中 写你的代码
4. 完成之后 提交到本地
5. 然后在右击仓库的文件夹 选择svn 导入